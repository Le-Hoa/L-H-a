var firstItem = documnet.getElementById('one');
id (firstItem.hasAttribute('class')){
    var attr = first.getAttribute('class')

    var el  = document.getElementById('scriptResults ');
    el.innerHTML = '<p>The first item has a class name: ' + attr + "</p>"
}